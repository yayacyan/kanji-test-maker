# 漢字テストメーカー 評論（2026-09-20）

三個角色（家長／補習班老師／小學生）各由一位獨立評審實際操作後，再由我彙整。
這份是「評論」，不是修改紀錄。做到哪、怎麼開，請看 `STAGE_NOTES.md`。

---

## 1. 一句話總評

**規格做得齊、版面像學校考卷，但「好不好用」還沒到位。**
給「自己會用工具的老師」現在就能用；直接交給家長或小孩，還差幾個關鍵處：用語、格子大小、指示文、首次畫面。

三位評審**各自都給 3／5**，而且挑出的問題高度重疊——這代表問題不是個別口味，是真的。

| 角色 | 評分 | 他們一句話 |
|---|---|---|
| 家長（佐藤さん） | 3／5 | 「何ができるアプリかすぐわかった。でも『抽題』『題目』『題型』はわからないし、間違えた漢字だけで作ろうとすると全部自分で打たないといけない。」 |
| 補習班老師（山本先生） | 3／5 | 「準備は1時間→1分になる。でもまだ塾のテストの体裁ではない。小テスト・練習用なら使う。正式なテストは Word で手直しが必要。」 |
| 小學生（はると，小3） | 3／5 | 「下線はなにをすればいいの？　マスがちっちゃくて、えんぴつだとはみ出しちゃう。」 |

## 2. 這次評審怎麼做的（以及限制）

- 三位都是**獨立的 AI 評審**，各自用 Chrome 實際操作網站、看畫面截圖、把 PDF 轉成圖片來看；沒有拿到我的實作說明。
- **小學生那位是「AI 模擬小學生」**。報告中已區分「量到的事實（格子幾 mm 等）」和「推測小孩會怎麼想」。真正的兒童反應**沒有測**。
- 都沒有測：iPhone／Android 真機、真的印表機、真實的練習簿照片、真人。
- 原始報告全文在 `reviews/`（`persona-parent.md`、`persona-teacher.md`、`persona-child.md`）。
  報告裡提到的截圖與 PDF 在暫存資料夾，之後可能消失；結論與數字已寫進本文。

---

## 3. 三方都指出的共同問題（最可信）

| # | 問題 | 誰指出 | 證據 |
|---|---|---|---|
| A | **答題格太小** | 三位都指出 | 實測：A4 直式 10 題＝6.0mm、30 題＝4.95mm、50 題＝4.25mm。小三好寫的大小約 10〜14mm（老師的經驗值，未經標準驗證）。A4 文字上限是程式碼寫死的 6.6mm，格子最大只會到 8.25mm；只有 A3 橫 10 題到 10.5mm |
| B | **A4 直式 10 題時下半頁大半空白** | 三位都指出 | 內容只到上方約 120mm，約 55% 空白；手動改 2 段就變成格子 7.8mm、字 17.7pt，但「自動」沒這樣做 |
| C | **沒有指示文，題型也沒分組** | 小學生、老師（家長間接） | 「書き」和「読み」預設交互排列，外觀又很像（都是畫線的語句）；沒有「請把畫線的ひらがな寫成漢字」這類說明。老師想要「書き 20＋読み 10」也做不到，只能 15：15 |
| D | **日文用語有中文腔** | 家長（最詳細） | 「抽題」「題目」「題型」「題庫」在日語裡不自然；「OCRエンジン」「Tesseract.js」「APIキー」「プロンプト」「JSON」對家長是術語。家長報告附有 23 組改寫建議 |
| E | **不能保存、複製、換裝置** | 老師、家長 | 只存在這個瀏覽器；再次抽題會蓋掉上一份；畫面上沒說「只存在這台裝置」，家長可能誤以為存在雲端 |
| F | **出題範圍只能選學年** | 老師、家長 | 不能「指定必出／排除某些漢字」、「避開上次出過的」、不能「只用孩子寫錯的字」（輸入純漢字只會變成描字的練習マス，題庫明明有讀音和例句卻不會自動補） |

## 4. 各角色獨有的重要發現

**家長**
- 首次畫面：一開就顯示預設的 10 題**見本**（1〜6 年混雜）＋「10問を抽題しました」提示（使用者什麼都沒按）。直接按「印刷」會印出見本；用手入力或 OCR 追加會變成「見本 10 題＋新題」，要先「全削除」。
- 手機開預覽時，紙張是實際大小的 60%，**第 1 題（在最右邊）被擠出畫面外**，要按看不懂的「全体」才修正。
- OCR 把「／」讀成「プア」「ノ」時，錯字**不經警告就能加進題目**；OCR 失敗訊息沒寫下一步。
- 解答紙：自由入力的範例答案只有約 6.5pt 紅字、沒標「例」；「子ども／子供」「友だち／友達」這種兩種寫法都對的詞，解答紙只給一種。

**補習班老師**
- 表頭只有名前、点；沒有日期、班級、配點、合計、補習班名稱。
- 沒有「大問見出し」；沒有 A／B 版；沒有按學生管理或「錯題再出」。
- 題庫 60 題（每學年 10 題隨機抽）：**讀音 60／60 正確**；例文只有 1 件不自然（潮：「海の潮」重複）；書き問題答案不易唯一的 4 件（辞典→事典、亡くなった→無くなった、名まえ→名前、皇后是敏感內容）。整體偏基礎、一字一語、例文短（12〜15 字）而單調。
- 小四題庫約 12% 是縣名用字（這是 2020 年學年別漢字配当表改訂的結果，不是錯）；同一份卷子可能同時出「岐」「阜」兩題，答案都是「岐阜県」。
- 貼上功能寬容度好：Excel 20 行、「漢字（よみ）／例文」10 行全部正確；表頭自動排除。

**小學生**
- 實測之外的推測：例文穴埋め的方框旁邊有小小的讀音（約 7.5pt），孩子會以為那是答案；練習マス不知道寫幾遍；「○○をつかって、文をつくりましょう」的書寫欄很窄（約 5.5〜8mm），而且線的長度剛好等於範例答案的長度，等於洩漏答案長度。
- PDF 中格子的十字輔助線各格不一致（有的只有橫線、有的沒有），可能與檢視器有關（未確認）。
- 好的地方：縱書、右到左、圓圈數字很像學校考卷，「不可怕」；格子數＝答案字數（含送假名）是好的提示；小一用平假名為主、小六用「憲法」「鋼鉄」之類學年相符的詞。

---

## 5. 我自己的評論

### 做得好的
1. **縱書考卷的完成度高**：右起排列、圓圈數字、紅字解答紙、A4／A3 尺寸正確、長句折行不切字，這是難處，做出來了。
2. **題庫的骨架可信**：學年配當用官方表核對、機械檢查全數通過、日文二讀只挑出 5 件要修，老師抽樣 60 件讀音全對。
3. **輸入的容錯度高**：多種貼上格式、AI JSON、OCR 先編輯再加入，這個「不直接產生題目」的設計，也被實際的 OCR 誤讀證明是對的。
4. **技術面穩**：無橫向溢出、按鈕≥44px、自動保存可靠、輸入不會被當成 HTML 執行。

### 做得不好的
1. **我把「不溢出」當成了排版的目標，而不是「小孩寫得下、看得懂」**。文字大小上限 6.6mm 與自動段數，是我為了讓版面不破而定的，沒有從「小三的手寫格」去定。這是格子太小與下半頁空白的直接原因（A、B）。
2. **沒有指示文和題型分組**：我照「題型可選」做成了功能，但沒有想到「考卷是給沒有老師在旁邊說明的人看的」（C）。
3. **日文介面沿用了中文詞**：「抽題」「題目」「題型」「題庫」原本的舊版程式就有，我沿用而沒有改成自然的日文。這是我漏掉的。
4. **首次畫面把「見本」當展示，卻沒替「只想做自己的卷子的人」想**（預設 10 題會殘留、會被印出來）。
5. **我的驗收全通過，但只驗了「規格符合」，沒驗「好不好用」**。字型設定其實是壞的（見下節），連兩位驗收者也沒發現，是小學生評審逐項比對字型才抓到。

### 這次評審中已經修掉的
- **書體設定失效**：字型名稱裡的雙引號把 `style="..."` 屬性截斷，不論選教科書體／明朝體／黑體，全部都變成黑體。已改成單引號，三種書體現在都會正確套用，並重跑溢出與版面回歸測試（不受影響）。
- **注意**：這台 Mac 沒有「UD デジタル 教科書体」，所以選「教科書体」實際顯示的是**ヒラギノ明朝**。裝有該字型的 Windows／學校電腦才會顯示教科書體。畫面上目前沒有誠實標示這件事（家長也指出）。

---

## 6. 建議的優先順序

**先修（會誤導或出錯，小改動）**
1. 手機預覽一開始就「符合畫面寬度」，或預設捲到最右邊（第 1 題）；「全体」改成「画面に合わせる」。
2. 首次不要放見本，或明寫「これは見本です」，並且在追加前先問要不要清掉；不要一開就跳出「10問を抽題しました」。
3. OCR 出現「プア」「ノ」等可疑字元時警告；失敗訊息加「手入力で作る」的導引。
4. 「教科書体」在沒有該字型的裝置上標示「この端末では明朝体で表示」。
5. 「印刷 / PDF」旁標示現在印的是問題用紙還是解答用紙。

**再做（最影響「能不能交給小孩」）**
6. **格子放大與自動段數**：10 題 A4 直式自動 2 段；文字上限提高；題數多時提示「A3 橫比較合適」。目標：小三用格子至少 8〜10mm。
7. **指示文＋題型分組**：依題型自動加大問標題與說明（例：「一　線のひいてあるひらがなを、漢字で書きましょう。」），可開關；預設改成「書き→読み」分組而不是交互。
8. **日文用語改寫**：抽題→問題を作る、題目→問題、題型→問題の種類、OCRエンジン→隱藏、API・プロンプト・JSON→拿掉或改白話。家長報告 `reviews/persona-parent.md` 有完整對照表。

**之後**
9. **「只輸入漢字就自動補讀音與例句」**（家長最主要的情境；題庫已有資料）。
10. 保存／複製／JSON 匯出匯入、A／B 版；表頭加日期、班級、配點、合計。
11. 出題進階條件：必出／排除、避開上次、排除縣名、題型比例、自由件數。
12. 題庫：別解（友だち／友達）、敏感字開關、把老師點出的 4〜5 件例文修一修。

**建議的做法**：第 1 批（1〜5）可以一次做完，工作量小；第 2 批（6〜8）是把「工具」變成「可以交給別人的成品」的關鍵，我建議接著做，做完再跑一輪相同的三角色評審，用同樣的問題比較前後。第 3 批看使用場景再決定。

---

## 7. 附錄

### 7-1. 各用紙的實測尺寸（小學生評審量測，三年級、書き＋読み交互）

| 用紙／題數 | 段×列 | 文字 | 解答格一邊 | 備註 |
|---|---|---|---|---|
| A4 直 5 題 | 1×6 | 6.6mm（18.7pt） | 8.25mm | |
| A4 直 10 題 | 1×10 | 4.8mm（13.6pt） | 6.0mm | 下半頁空白約 55% |
| A4 直 10 題（手動 2 段） | 2×6 | 6.24mm（17.7pt） | 7.8mm | |
| A4 直 20 題 | 2×10 | 4.8mm | 6.0mm | |
| A4 直 30 題 | 3×10 | 3.96mm（11.2pt） | 4.95mm | |
| A4 直 50 題 | 5×10 | 3.4mm（9.6pt） | 4.25mm | 番號數字約 6.7pt |
| A4 橫 10 題 | 1×10 | 6.6mm | 8.25mm | |
| A4 橫 30 題 | 3×10 | 4.9mm | 6.12mm | |
| A4 橫 50 題 | 4×13 | 3.65mm | 4.56mm | |
| A3 橫 10 題 | 1×10 | 8.4mm（23.8pt） | 10.5mm | 唯一超過 10mm |
| A3 橫 30 題 | 4×8 | 5.7mm | 7.12mm | |
| A3 橫 50 題 | 4×13 | 5.5mm | 6.87mm | |

格子＝文字的 1.25 倍；文字上限程式碼寫死 A4=6.6mm、A3=8.4mm。

### 7-2. 日文用語改寫（家長評審 23 組中最重要的 10 組）

| 現在 | 建議 |
|---|---|
| 抽題する／学年から抽題 | 問題を作る／学年から問題を選ぶ |
| 題目／題目一覧／題目に追加 | 問題／問題一覧／問題に追加 |
| 題型 | 問題の種類 |
| 書き問題／読み問題 | 書き取り／読み |
| 例文穴埋め | 例文の空欄（□に漢字を書く） |
| 練習マス／自由入力 | なぞり・写し書き／文づくり |
| 問題紙／解答紙 | 問題用紙／解答用紙 |
| 全体（縮放按鈕） | 画面に合わせる |
| OCRエンジン／Tesseract.js | （隱藏）「縦書きの写真のときはチェック」 |
| APIキー／プロンプト／JSON | 「お使いのAI（ChatGPTなど）に貼り付けて、返ってきた答えをここに貼ります」 |

### 7-3. 沒有驗證到的（誠實清單）

- 真人小孩、真人家長、真人老師的反應（這次全是 AI 模擬）。
- iPhone Safari／Android 實機、真實印表機、印表機邊界。
- 真實的練習簿照片與縱書き的 OCR。
- 「小三適合 10〜14mm 格子」是老師評審的經驗值，不是正式標準；要定案前，建議實際拿一張印出來給孩子寫寫看。
- 小一〜小四題庫的讀音只被抽樣（每學年 10 件）確認，小五、小六約 384 個詞被目視全數確認無誤。


---

## 8. 第2輪の結果（修正後の再評価）

同じ3つの役柄の評審に、**前回の自分の報告書を読んだうえで**同じ課題をやり直してもらい、機能の回帰検査も別に1人で行った。

| 角色 | 前回 | 今回 | 前回の指摘の内訳 |
|---|---|---|---|
| 家長 | 3／5 | **4／5** | 困擾點15件：解決8・一部7・未解決0 |
| 補習班老師 | 3／5 | **4／5** | 解決2・一部6・未解決（題庫の傷、別解など） |
| 小學生（AIの模擬） | 3／5 | **4／5** | 13件：解決5・一部5・未解決3 |

機能の回帰検査（20条件）：18通過。不通過だった2条件のうち、鑑などの警告は直し、極端な条件でのはみ出しも直して再検証済み。

**良くなった点（評審の言葉）**
- 「見本が消えて『問題を作る』だけになった」「巣歩観だけで作れるようになった」（家長）
- 「書き20＋読み10が6操作で作れる／大問ごとにまとまる」（老師）
- 「説明があるから先生にきかなくてもわかる。マスも大きくなった」（はると）

**第2輪で新しく見つかった重要な不具合**
- 印刷でだけ解答マスが切れる（画面と印刷の高さの違い）→ 修正済み。以後、印刷メディアでも実測する。
- 書き取りの答えの漢字が、同じ用紙の別の問題文にそのまま出る（例：答え「静」に「静岡県」）→ 入れ替えで減らした（完全には防げない）。

**残っている課題（優先度順）** … `STAGE_NOTES.md` の11章に一覧。特に、30〜50問のマスが4〜6mmであること、題庫の指摘例文の修正と別解の扱い、複数テストの保存とA/B版。

**今回も未確認**：真人の児童・保護者・講師、iPhone/Android実機、実プリンタ、実際の練習簿の写真。評審は全員AIであり、「本当に使いやすいか」の最終判断は、実際に印刷して使ってみることでしか確かめられない。

## 9. 第3輪の結果（方格の位置・題型の追加のあと、4人での再評価）

前回と同じ3役に、**通りすがりの人（田中さん）**を加えた4人が、設計の経緯（ソースコード・メモ）を見ずに、画面だけでサイトを使って報告した（報告書の全文：`reviews/round3/`）。田中さんは「URLだけ知っている初めての大人」で、スマホから入る。

| 角色 | 第2輪 | 第3輪 | 一言 |
|---|---|---|---|
| 家長（佐藤さん） | 4／5 | **4／5** | 「巣借芽旅鏡」を貼るだけで3タップ・例文つき5問。方格は問題の隣（約8.7mm）。ただし同じ字を書きと読みで入れると答えが見える |
| 補習班老師（山本先生） | 4／5 | **4／5** | 条件付きで使える。A3横20問は毎週使いたい。名前付きの保存とA4の30問超のマスの小ささが課題 |
| 小學生（はると・AI模擬） | 4／5 | **4／5** | 説明があって迷わない。マス6.2mmは小さい。送りがな・練習マスの「残りのマス」に何を書くか分からない |
| 通りすがり（田中さん） | — | **4／5** | 3タップ・約1分で印刷用PDFまで到達。最初のプレビューが切れて壊れて見えた |

星の数は同じでも、**指摘の中身は変わった**（方格の位置・題型は誰からも指摘されなくなり、「使い始め」と「運用」の問題に移った）。

**4人に共通して出た問題と、今回の対応**
| 問題 | 誰が | 対応 |
|---|---|---|
| スマホで最初のプレビューが60%で、用紙の左半分と見出しが切れる | 家長・小學生・路人 | 最初から「用紙全体が見える大きさ」に変更（拡大は＋ボタン） |
| 通知がすぐ消える・下のボタンを隠す | 家長・路人・老師 | 画面上部に出して、表示時間を長くした |
| 何のサイトか、最初の画面で分からない／プレビューが空 | 路人・家長 | 1画面目に用途の説明を追加。プレビューが空のとき「見本の用紙を見る」ボタン（7種類の見本） |
| マスが小さい（A4に多問） | 小學生・老師・家長 | 「約○問までにすると8mm以上」の目安を表示。□のマスを他と同じ大きさにし、補助線も追加。低学年向けの説明は既存 |
| 「保存済み」がスマホで見えない | 家長 | スマホでもヘッダーに表示 |
| 説明が少ない／専門的な題型名 | 路人・家長 | 用紙の説明文は既存。題型のやさしい説明は未対応（下の残課題） |

**そのほか直したこと（1人の指摘）**
- 同じ漢字を書きと読みで入れた／同じ問題が2つあるとき、「問1の答え『巣』が問7に出ています」の警告（家長）。貼り付けで同じ問題が続くときは、重ねて追加しない。
- 「借（か）りる／例文」の形の貼り付けが、例文を壊していた → 正しく読めるように（家長）。「／」が例文に残っていれば警告。
- 「並べる段の数」を手動で1段にして50問にすると紙面が崩れる → 読める段数に自動で直して、その説明を表示（老師）。
- 名前をつけて保存・呼び出し（小3 第5回 A など）。1問の削除は「元に戻す」付き。呼び出しも「元に戻す」付き（老師）。
- 練習マス・なぞり書きのマスの数を「字数の倍数」にして、解答用紙はお手本を全マスに繰り返す。説明文は「くりかえし書く」に（小學生）。
- 教科書体：この Mac にある「游教科書体」が指定に入っていなかったため明朝体になっていた → 追加（小學生）。
- ブラウザの「戻る」で、サイトから出ずに前の画面へ（路人）。開いた項目が画面外なら、そこまで移動（路人）。
- 種類を6〜7種類混ぜても「問1〜○」を付ける（31問以上は場所がないため従来どおり短くまとめる）（老師）。

**今回は直さなかったこと（理由）**
- 教科書の一節から漢字を拾って問題にする流れ（老師）：新機能で、別の設計が必要。
- 読みの欄が長い問題の文の途中の空き（3人が指摘）：直すと全体の文字が小さくなる（試して戻した）。
- 種類ごとの配点、ドラッグでの並べ替え、設定を変えながらプレビューを見られる配置（老師）。
- 題型名の図や例つき説明（路人・家長）、「印刷」の実機での案内（路人）。
- 同訓異字・部首・画数などの追加題型：正しいデータがない。

**もともとある限界（今回の変更が原因ではない）**：A4横50問で長い文が多いとき、文字サイズの下限(2.8mm)で、約1/36の頻度で文が列からはみ出す（旧版に同じ問題セットを入れても同じ場所で再現）。

**最終調整の独立検査（別担当、条件17項目）**：16項目は通過、1項目（通知内のボタンが高さ36px＝44px未満）が不通過 → 44pxに直して再確認した。同じ検査で指摘された小さな点も直した：
- 「約○問までにすると8mm以上」の目安が実測とずれることがあった（案内は18問、実測7.4mm）→ 余裕を見て数えるようにし、A4縦30問・A4横40問・A4縦20問の各3回で、案内どおりにすると8mm以上になることを確認。
- 練習マスだけ他より4%大きかった → 同じ大きさに。
- すでにA3横のときも「A3や横向きに」と出ていた → 用紙に合わせた案内に。
- 重複だけの貼り付けで「0問を追加しました」と出ていた → 「同じ問題がすでに入っているので、追加しませんでした。」に。
- 通知がスマホでプレビューの操作バーを覆う → タップで消せるように。
※不通過だった1項目とその小修正の確認は、独立検査担当ではなく私自身の再テストで行った（独立検査の再実行はしていない）。

**今回も未確認**：真人、iPhone/Android実機（「印刷 / PDF」を押したあとの画面）、実プリンタ、実際の写真のOCR、ChatGPT等との実際のやりとり。評審は全員AIである。

## 10. 第4輪（画面デザインの全面改版 → 4人で再評価 → 最終調整）

**きっかけ**：「今のページはAI感が強く、直感的でもない。siteinspire の Idle のような設計にしたい。ただし使いやすさは保つ」。参考サイトの特徴（冷たい白の背景、黒い丸ボタン、浮かぶ丸いナビ、大きな太字の見出し、余白、差し色は1色）を見て、画面まわりだけを作り替えた。**考卷（印刷結果）は変えていない**（旧版と新版で、6通り〔A4横・縦・A3横 × 問題・解答〕を画素単位で比べて差0）。改版前の一式は `backup-before-ui-redesign-20260920-1623/` に保存。

**最初の改版でやったこと**
- 入口を整理：「4枚のタイル＋同名の折りたたみ4つ」の二重構造をやめ、「学年から／自分で／写真・AI」の3つの選択に一本化。選ぶと、その入力欄だけが出る。
- 浮かぶ丸いナビ、下の切り替えバーも浮かぶ丸いバー、通知は黒い丸い形。
- 電脳版で設定を開くとプレビューが7%まで縮む問題 → 設定を用紙の上に重ねて出す方式に変更。
- CSSを「画面用（styles.css）」と「用紙用（paper.css）」に分離。用紙側は1文字も変えていない。

**4人の評価（第4輪）**：家長4／老師4／小學生4／路人4（全員★4）
- **共通した指摘（4人全員）**：画面の外側が「生成系サイトの雛形」に見える（黒い丸ボタン、01/02/03のカード、黄色のマーカー、浮かぶバー、「〜を、数タップで」という一般的な見出し）。一方、考卷そのものは「学校の紙を分かった人の作り」（縦書き・教科書体・名前欄・警告文）。→ 依頼の「AI感」が、まだ画面の外側に残っていた。
- スマホの最初の画面で、浮かぶ「問題を作る」が問題の種類のボタンに重なる（3人）。
- スマホの「印刷 / PDF」から、問題用紙か解答用紙か分からない（1人）。
- マスが8mm未満になる目安を、問題数を選ぶ時点で出してほしい（老師・小學生）。
- 「答えが見える」警告が出るが、直し方が分からない（家長）。
- 初回なのに「前回の問題を入れ替えました」と出て不安（路人）。

**最終調整（この輪）**
- **AI感の対策（見た目の「学校らしさ」を足した）**：
  - 差し色を黄色から朱（先生の赤ペン、解答用紙の赤字と同系統）に。背景を冷たい灰色から、温かい紙の白に。ぼかし（ガラス風）をやめた。
  - ロゴと3つの選択の飾りを、用紙のマスと同じ「十字入りの四角（田字格）」に。数字（01/02/03）をやめ、「学」「書」「写」の漢字を入れた。
  - 見出しを、用途の分かる言葉に（「縦書きの漢字テストを、その場で作る。」）。横に、できあがりの縦書きの見本（□に赤字の「巣」、読み「す」）を置き、開いた瞬間に何が作れるか分かるように。黄色のマーカー線はやめた。
  - 補助の文字を大きく・濃く（11〜12px・薄い灰色 → 13.8px・濃い色）。
- **使い勝手**：
  - 「問題を作る」を浮かせるのをやめ、種類の選択肢のすぐ下に置いた（重なりの解消）。スマホの最初の画面で、種類の選択肢が全部見える。
  - スマホの「印刷 / PDF」ボタンに、印刷される用紙（問題用紙／解答用紙）を常に表示。
  - 「答えのマスを8mm以上にするめやす：A4縦は約10問、A4横は約14問、A3横は約28問まで」を、問題数を選ぶ場所に表示（最初は「A3横は50問まで」と書いたが、独立検査の実測で約6mmと分かり、実測に合わせて直した）。
  - 「答えが見える／同じ問題」の警告に、「問○を消す」ボタンを追加（消したあとは「元に戻す」が出る）。
  - 初回は「入れ替えました」を出さず、2回目からは「前回に作った問題10問は入れ替えました」と、数を入れて表示。

**最終調整の独立検査（別担当、条件11項目）**：10項目は通過、1項目（条件4：目安の文とマスの実寸のずれ）が不通過。
- 不通過の中身：私が書いた目安「A3横は50問まで」は、実測では約6mmで、8mm未満だった（A3横で8mm以上になるのは30問前後まで）。→ 実測（A3横20問9.2mm／28問8.4〜8.9mm／30問7.5mm前後）に合わせて「約28問まで」に直し、6回測り直して確認。A4縦10問（8.7〜9.0mm）とA4横14問（平均約8mm）の目安はそのまま。
- 通過した中身：考卷の画素差0（旧版と新版、24組）／はみ出し36回0件／375pxで横スクロールなし・44px未満の操作部品0／JSエラー0／名前つき保存・元に戻す・戻るボタン・見本ボタン・重複スキップは新デザインでも動く。
- 指摘されて直した小さな点：目安の文の「にするめやす」→「目安」、見本の文を「巣を作る。」に、空の用紙の案内が古いタイル名（「学年から問題を作る」）のままだったのを新しい名前に、緑色が2か所（自動補完の表示・写真読み取り成功）に残っていたのを中間色に、空の用紙に同じidが2つあったのをclassに。
- ※ 上の小さな修正の再確認は、独立検査担当ではなく私自身の再テスト（全テスト再実行）で行った。

**直さなかったこと**
- 老師：配点を種類ごとに分ける／解答の縮刷版／大きい字の答え一覧、1問だけの差し替えボタン、A4で30〜50問のマスの小ささ（用紙の物理的な限界。目安の表示で対応）。
- 老師・小學生：読みの問題の文の途中の空き（直すと全体の文字が小さくなる）。
- 小學生：説明文と送りがなの読みが約8pt（用紙の面積との兼ね合い。今回は用紙を変えない方針）。
- 路人：「熊本」と打つと「熊本県」＋「本」に分かれる（漢字の並びを1字ずつ分ける仕様による。曖昧な入力は自動判定できない）。
- 設定を開いたときのプレビューの隠れ具合（重ね表示にしたが、用紙の上部は隠れる）。

**この輪でも未確認**：iPhone/Androidの実機、実プリンタ、Windows等で教科書体が入っていない端末での見た目。評価者は全員AIで、「AI感」の判断も、AIの目による印象である。

## 11. 第5回（4人のテスト役と修正）

`reviews/round5/summary.md` に、指摘・修正・検査結果・直していないことをまとめた。

## 12. 第6回（5人のテスト役：山本先生・佐藤さん・はると・田中さん・佐々木先生（72歳））

5人の指摘のうち、共通して出たものを直した（★は3〜4で割れたので、内容で比べた）。第5回のあとに手元で作っていた次の2点と一緒に、2026-09-21に公開した。
- 「自分で」画面の種類カードの絵が、「答えを書く場所」（マス／かっこ／線）に合わせて変わる。
- 種類が変わる最初の問題の上に、小さな見出し「ここから ○○」を付ける（種類のまとまりが2つ以上あり、同じ種類が離れて2か所に出ていないときだけ。その段は全問の番号の高さをそろえる）。

直したこと
- 種類が交錯しているとき（書き→読み→書き…、シャッフル後）：上の説明文を「問1〜2・5〜6　…」のように種類ごと1行にまとめる（以前は1問ずつ8行に増えた）。「ここから」は付けない。
- 問題一覧に「種類ごと」ボタン（同じ種類を続けて並べる）を追加。
- 「ここから」の見出しの言い方：書き取り／読みがな／文中の□／送りがな／練習マス／なぞり（1〜2年はひらがな）。「漢字」が「漢字を読む」とも取れる問題を解消。
- 見出しの文字に下限（2.4mm）。30問でも約6.8pt。文字が2.8mm以下の極小の用紙では従来どおり。
- 学年に「小学1〜2年」、問題数に「15問」を追加。
- 「約10問／約7問」の食い違い：作る前の案内を「10問くらいまでがおすすめ（種類や文の長さで変わる）」に。作ったあとの警告は「8mm」の言葉をやめ、「書きやすい大きさになります」に。
- 画面の薄い小さい字（--faint、見本・欄の注記）を少し濃く・大きく。
- 独立検査（10項目）で唯一の不通過だった「交錯時の説明文が中途半端に折り返す」を修正：番号が長いときは、番号の行と説明の行を分けて2行にし、高さの見積もりも合わせた。修正後、シャッフルした36通り（用紙3×種類3組×問題数4）で説明文の食い込み0、はみ出しは既知の1件のみ。

既知の制限：A4横・50問のように文字が下限2.8mmに達する用紙で、まれに1問だけ列からはみ出す（修正前の版でも同じ条件で起きることを確認済み。乱数の抽題により出たり出なかったりする）。

直していない（次回以降の候補）
- 30問・6種類のA4は、文字約9pt／ふりがな約5ptが物理的な限界（警告は出る）。段を増やしても、長い文が折り返して逆に小さくなる（自動選択が実測済み）。
- スマホで「問題を作る」後の通知が上部を隠す／自動でプレビューに移らない。
- 文末1〜2字（「ぐ。」）だけが次の列に落ちる。
- 「まとめて貼り付け」が「答えを書く場所」の設定を無視してマスになる。
- 自動の例文が入らない語（「開く」「歩ある」）に警告がない。
- 最初の種類に「ここから」を付けるかどうか（今は付けない）。
- 説明文が「漢字を書きましょう」でも、答えのマスに送りがな（ひらがな）が入る問題がある。

## 13. 第7回（公開後に届いた、iPhone 実機の指摘。未公開）

公開版（第6回）を iPhone で使った人から、画面キャプチャつきで次の4点が届いた。すべて手元で直した（公開はまだ）。

1. **キーボードが出ると、下の「編集／プレビュー／解答」の帯が画面の途中に浮く** → 文字を入力している間は帯を隠す（入力が終わると戻る）。
2. **□に付く読みがなが、□の枠に重なる**（Safari。Chrome では貼りつく程度）→ Safari は □ を文字より右に置くため。□を文字と同じ基準線にそろえ、読みがなの左に余白（0.24字）をとった。WebKit・Chrome の両方で、読みがなと枠の間が空くことを実測。
3. **印刷ボタンを押しても何も起きない** → LINE・Discord などアプリ内のブラウザは印刷に対応していないことがある。印刷の画面が出なかったときは、理由と「Safari／Chrome で開き直す」案内、「URLをコピー」ボタンを出す。確認の画面が出せない環境（答えが見える問題があるとき）は、通知の「このまま印刷」から続けられる。
4. **「編集して閉じてもプレビューが変わらない」** → 送りがな問題で、漢字欄に「借」、答え欄に「りる」と入れたケース。プレビューは更新されていたが、「借りる」として読めないため、□のうしろに「りる」がそのまま残っていた（答えが見える）。漢字欄が「借」だけでも、答えが「りる」で、例文に「借りる」があれば、「借りる」として扱うようにした。答えが違う（「か」）ときは従来どおり警告を出す。

確認：WebKit（iPhone 13 相当）と Chrome で、上の4点の再現→修正後の状態を実測。既存の検査一式と、用紙のはみ出し検査（96通り）は通過（文字が下限の極端な用紙 A4横50問のみ、従来どおりまれに1問はみ出す）。**独立検査は未実施。**
注意：実際の iPhone のキーボードと、アプリ内ブラウザの印刷は、この環境では再現できない。上の3点は、iPhone での実機確認が必要。


## 14. 第7回・追記（送りがなの入力欄に、その場で分かる説明を追加。未公開）

第7回の(4)は「入力の書き方が違っていても、それらしく直す」対応だった。それに加えて、入力する時点で迷わないよう、「自分で作る」フォームと、問題一覧の編集欄の両方に、問題の種類ごとの説明を出すようにした。

- 種類（書き・読み・□に書く・送りがな・練習・なぞり・自由入力）を選ぶと、「漢字」「よみがな」欄のラベルとプレースホルダー（例：借りる／かりる）、下の説明文が、その種類に合わせて変わる。
- 送りがなは「『借りる』のように、漢字のあとにひらがなが続く語を、そのまま『漢字』に入れてください（『借』と『りる』に分けて別の欄に入れる必要はありません）」と明記。
- 問題一覧で「編集」を開いたときも、同じ説明が出る（そこで入力を直すときにも見える）。

確認：種類7つすべてで、ラベル・プレースホルダー・説明文が切り替わることを実測。既存の検査一式は通過。独立検査は未実施。

## 15. 陳さんの指摘と、格子の大きさについて（未公開）

第7回公開後、新しいテスト役（在日台湾人の母親という設定）に公開サイトを試させたところ、縦書きの文が2列目に折り返るとき、残った1〜2字が隣の問題番号のすぐ上に浮いて見える、との指摘があった。また、利用者本人（Yaya）からも「マスが小さい」との感想があった。

**文の折り返しについて：** 再現して調べたところ、残った文字は自分の問題の枠からはみ出してはおらず（実測で確認）、番号と同じ高さから2列目が始まる縦書きの仕組み上、隣の問題の番号と近い位置に来ることが原因と分かった。「最後の数字だけをひとまとまりにする」対策を試したが、30〜50問の極端な用紙で新たにはみ出しが起きたため見送り、区切りの線を少し濃く太くする対策のみ採用した（安全確認済み）。根本的な解決には、折り返しの列を作り直す設計変更が要る。次回以降の課題として残す。

**マスの大きさについて：** 本人に確認したところ、(1) 一般の答えマスと練習マスの両方が対象、(2) 対策として問題数を減らすより、A3や横向きの用紙に変える方を選びたい、とのことだった。調べると、A3横は同じ問題数でもA4縦よりマスが3〜4割ほど大きい（例：書き取り10問でA4縦8.7mm→A3横12.3mm）。この選択肢は元からあったが、印刷直前の「プレビュー」画面にしかなく、気づきにくかった。「学年から問題を作る」の最初の画面（問題の数のすぐ下）にも同じ「用紙の大きさ」を選べるようにし、そちらで変えるとプレビュー側にも反映される（相互に同期・保存も維持）。

確認：新しい用紙選択の相互同期・保存後の復元、既存の検査一式、96通りのはみ出し検査（0件）。独立検査は未実施。

## 16. 送りがなの読みがなが、漢字から離れて見える（未公開）

利用者本人から、送りがな問題の「借」に付く読みがな「か」が、「借」から離れて見える、対応が分かりにくいとの指摘。

原因：iPhoneの指摘対応（第7回）で、読みがなが□の枠に重ならないよう rt に左の余白（0.4em）を足したが、この余白が「文中の□」（読みがなの土台が□そのもの）と「送りがな」（読みがなの土台が漢字の字で、□は外側の別要素）の両方に一律にかかっていた。送りがなのほうは□と重ならないので、この余白は不要で、かえって漢字と読みがなの間を空けてしまっていた。

対応：土台が□のときだけ余白が付くよう、専用のクラス（hint-ruby-box）に分けた。送りがなの「か」は「借」にくっつき、文中の□の読みがなは従来どおり□の枠と重ならない（実測：Safari 3px、Chrome 6.1px の余白を維持）。

確認：両方のケースを画面・印刷状態で実測。既存の検査一式・96通りのはみ出し検査（既知の極端な例のみ）は通過。独立検査は未実施。

## 17. 測試員回饋修復（未公開）

3人のテスト役（塾講師／初見の通りすがり／在宅の保護者）が手元版（第16節まで当てた版）を試し、9件の指摘が出た。すべて手元で直した（**公開はまだ。git には触れていない**）。
変更したファイル：`index.html` `app.js` `styles.css`（`paper.css` `import.js` `kanji-db.js` は無変更）。変更前の6ファイルは `backup-unpublished-20260922d/` に退避。
検査は Chrome（Playwright、`channel='chrome'`）で、画面と**印刷メディア**（`emulate_media('print')` ＋ `page.pdf(prefer_css_page_size=True)`）の両方で実測。合計89項目すべて通過。印刷成果物の画像は `samples/round8/`。

### 1. 答えが見えてしまう警告が、読まずに押せてしまう（致命）

- 問題：保護者のテストで、30問の混合出題の1回目から「問25の答え『吸う』が問18に出ている」が実際に起きた。警告は `window.confirm` で、「OK」が押しやすく、中身を読まずに印刷してしまう。
- 原因：ブラウザ標準の確認は、危ないほうの選択肢（このまま印刷）が既定の位置にあり、文言も「OK」でしかない。
- 対応：画面内の自作ダイアログ（`index.html` の `#printConfirm`、`styles.css` の `.modal` 一式、`app.js` の `openPrintConfirm`／`closePrintConfirm`／`printConfirmKeydown`）に置き換えた。
  - 「戻って直す」＝黒い大きなボタン（既定でフォーカスが当たる）、「このまま印刷する」＝下線だけの小さな文字。
  - Esc・外側（うすい黒の部分）のタップ・開いた直後のEnterは、すべて「戻って直す」（印刷しない）。
  - 警告の文は省略せず全文を出す。見出しは、答えが別の問題に出ているときは「答えが見えてしまう問題があります」、同じ問題が2つあるだけのときは「同じ問題が2つ入っています」。Tabはダイアログの2つのボタンの中だけを回る。
  - 警告がないときはダイアログを出さず、そのまま印刷する（毎回じゃましない）。
  - 第13節(3)で入れた「確認の画面が出せない環境向けに、通知から続ける」回避策は不要になったので外した（自作のダイアログはアプリ内ブラウザでも必ず出るため）。
- 確認：答え漏れ・重複あり／マスが小さい／両方／警告なし の4通りで、出る・出ない、Esc・外側クリックで印刷されないこと、「このまま印刷する」でだけ印刷が走ることを実測（`window.print` を数えて確認）。スマホ幅（390×844）でもカードが画面内に収まり、2つのボタンが見えることを座標で実測。画像：`samples/round8/1〜3`。

### 2. 練習マス・なぞり書きで、同じ段の列の高さがそろわない（致命）

- 問題：「山」（1字＝4マス）と「学校」（2字＝8マス）が同じ段に並ぶと、マスの列の高さが倍ちがい、紙面の下に大きな空きができて不ぞろいに見える。
- 原因：マスの数は「字数 × 書く回数（1列10マスまで）」で問題ごとに決まり、段の中でそろえる仕組みがなかった。
- 対応：`app.js` に `practiceCellCount()` と `alignPracticeCells()` を追加し、`buildSheet()` の中（**問題のHTMLを作る前**）で、段ごとに練習マス・なぞりの最終的なマス数の**最大値**にそろえる。そろえる先はその段に元からある数なので、紙面からはみ出さない。マスの中身（どの字が何番目に入るか）は既存の循環の規則のまま。同じ段に練習マスが1問しかないときは、従来どおりそのまま。
- 確認：「山／学校／川／公園／空／先生」を練習マス・なぞりで6問（A4縦1段）：修正前 `4,8,4,8,4,8` → 修正後 `8,8,8,8,8,8`、マスの列の高さも 145.5px/289.88px の混在から 289.88px にそろった（印刷メディアで実測）。12問（2段）でも段ごとに独立してそろうこと、解答用紙でも同じこと、練習マスが1問だけの段は元のまま（4）であることを確認。画像：`samples/round8/4_practice_cells_before.png`／`5_practice_cells_after.png`。

### 3. 「マスが小さい」の警告が、印刷を止めない

- 問題：`sheetInfoFor()` の文字の注意だけで、印刷はそのまま通っていた。
- 対応：注意文を `smallCellText()` に分け、`printWarnings()` から答え漏れと一緒に第1項のダイアログへ渡す。1つのダイアログに2件並ぶ（見出し「マスが小さめです（解答マス 約◯mm）」）。
- 確認：30問・A4縦（解答マス約5mm）でダイアログが出ること、答え漏れと同時のときは警告が2ブロック出ることを実測。画像：`samples/round8/3_kakunin_dialog_two_warnings.png`。

### 4. 「マスを大きくしたい」の案内が、いきなりA3をすすめていた

- 問題：A3は一般の家庭用プリンターでは印刷できないことが多く、最初の案内としては重い（コンビニ等が必要）。
- 対応：静的な案内（`index.html` の `.size-hint`）と、用紙の下に出る動的な案内（`app.js` `smallCellText()`）の両方を、**まずA4横**、それでも小さければA3横、の順に書き直し、A3には「家庭用プリンターでは印刷できないことがあります（コンビニなどの印刷が必要です）」を添えた。いまがA4横のときはA3横を、A3横のときは問題数を減らす案内になる（従来の分岐は維持）。
- 確認：A4縦の30問で、案内の中で「A4横」が「A3横」より先に出ること、「家庭用プリンター」の注意が入ることを文字列で実測。

### 5. 「送りがな」の説明が、用語をくり返しているだけだった

- 問題：種類カードの説明が「送りがなも書く／送りがなを書く」で、「送りがな」を知らない人には何も伝わらない。
- 対応：`app.js` の `TYPE_INFO.okuri` を、カードの説明「「借りる」の「りる」」、マウスを乗せたときの説明「漢字のあとに続くひらがなを書く（「借りる」の「りる」）」に変更。例はこのサイトが実際に使っている「借りる／かりる」に合わせた。種類の名前「送りがな」自体は変えていない。
- 確認：カードに名前「送りがな」が残り、説明が新しい文言になり、カードの中で文字があふれていない（`scrollWidth`＝`clientWidth`）ことを実測。

### 6. パソコンの広い画面で、「用紙の大きさ」が2か所に出る

- 問題：左の設定パネル（`#gradeFormatSelect`）と右上のプレビューバー（`#printFormat`）が同時に見え、どちらを使えばよいか迷う。
- 対応：左右が同時に見えるのは `@media (min-width: 1000px)` のときだけと確認したうえで、その中で `.preview-bar .bar-field` を `display:none` にした（JSの相互同期・保存はそのまま）。第15節で「A3に気づきにくい」を直すために足した左側のほうを残した。
- 確認：幅1440では右上が非表示・左が表示。スマホ幅390では、編集タブ＝左が見える／プレビュー・解答タブ＝右上が見える、と**実際にタブを切り替えて**確認（従来どおり）。

### 7. 「文中の□」の読みがなが既定で出ていて、答えが推測しやすい

- 問題：□の右の読みがなから答えが分かってしまう。一方、送りがなの読みがな（「借」の「か」）は漢字の読みだけなので答えは漏れず、保護者のテストでも好評だった。ところが両方が1つの設定（`readingHint`）で動いていた。
- 対応：`app.js` に `okuriReadingHint`（既定 true、画面の設定項目は作らず内部固定）を追加し、送りがな側（`segCoreHtml` の okuri、説明文、文字サイズ見積もりの ruby 判定）をそちらに移した。`readingHint` は既定 false にして「文中の□」専用にした（チェックボックスの文言は元から「例文の空欄に…」なので変更不要。保存・復元の対象にも従来どおり残す）。
- 注意：すでにこの端末で使っている人の保存データには `readingHint: true` が残っているため、その人の表示は変わらない（新しく使い始めた人・保存を消した人から既定オフになる）。`okuriReadingHint` は保存データに無ければ既定の true が使われるので、既存の人も送りがなの読みがなは出たままになる。
- 確認：既定の状態で、文中の□に読みがなが0件・送りがなに「か」が付くこと、説明文から「（□の右の小さい字は 読みがなです）」が消え送りがな側の案内は残ることを、印刷したPDFで実測。チェックを入れると□にも付き、**第16節で直した `.hint-ruby-box` の左余白（実測7.15px）は維持、送りがな側は0px**であることも確認（回帰）。画像：`samples/round8/6〜8`。

### 8. 「学年・組」「日付」の欄が既定で閉じていた

- 問題：複数の学級を受け持つ先生は毎週チェックを入れ直すことになる。
- 対応：`app.js` の `showClass`／`showDate` を既定 true に。見出しは2行（22mm）になるが、文字サイズの見積もりは元からこの高さを見ているため、はみ出しは出ない（下の検査で確認）。既存の保存データがある人の設定は変わらない。
- 確認：初期状態でチェックが入り、用紙の見出しに「学年・組」「日付」が出ることを印刷したPDFで確認。

### 9. 「自分で作る」で練習マス・なぞりを選ぶと、例文が紙に出ないのに入力欄がある

- 問題：入力しても捨てられるため、書いた人が混乱する。
- 対応：`app.js` の `applyCustomHint()` で、種類が `mas`／`trace` のときだけ「例文」のラベルと入力欄に `hidden` を付ける。入力ずみの文字は消さない（ほかの種類に戻すとそのまま出てくる）。
- 確認：7種類すべてを切り替えて、mas・traceでだけ隠れること、入力した「くもの巣がはる。」が全種類を通して残っていることを実測。画像：`samples/round8/9_custom_mas_no_sentence.png`。

### 回帰（前の回で直したものを壊していないか）

- 問題の区切り線：印刷状態で `border-left` が 1px（＝0.3mm）／`rgb(153,153,153)`（#999）のまま（第15節）。
- 送りがなの読みがなの余白分離（`paper.css` の `.hint-ruby-box`、第16節）：`paper.css` は**1文字も変更していない**。動きも上の7で実測確認。
- はみ出し：用紙3種×問題数10〜30×種類の組み合わせ5通り（練習マスだけ／混合）について、問題用紙・解答用紙の**印刷メディア**で、アプリ自身と同じ判定（問題の枠・用紙の外に部品が出ていないか）を実行し、0件。問題数も一致。
- 用紙選択の保存：左の設定でA3横にして再読み込み→両方の選択とも A3横・用紙寸法 420mm、プレビュー側で A4横に変えて再読み込み→両方とも A4横。

検査スクリプトと生成したPDF／PNGは作業用フォルダ（scratchpad）に保管。**独立検査は未実施。**

## 18. 極端高密度の組み合わせで、折り返した列が隣の問題に食い込む（未公開）

第17節のあと、独立検査で1件だけ残った不具合。**手元で直した（公開はまだ。git には触れていない）。**
変更したファイルは `app.js` **のみ**（`index.html` `styles.css` `paper.css` `import.js` `kanji-db.js` は1文字も変更なし）。変更前の6ファイルは `backup-unpublished-20260922e/` に退避。

### 指摘（独立検査の報告）

「小学1〜6年・50問・6種類すべて混合・A4縦」で、例文が長くて2列目に折り返す問題のとき、`.q-main`／`.q-text` の実寸が自分の問題の枠より左へ5〜9px はみ出し、区切り線を越えて隣の問題の列に入る。印刷したPDFでも確認できる。

### 再現

Chrome（Playwright、`channel='chrome'`）で、`emulate_media('print')` ＋ `page.pdf(prefer_css_page_size=True)` を通したうえで、**`#printArea`**（`#paperHost` は印刷では `display:none` で、測ると全部0になる）の中の `.q-main, .q-text, .q-ans, .q-ans > *, .tgt-wrap, .side, .side > *` を、それぞれが属する `.q` の枠と比べる（アプリ自身の `overflowing()` と同じ判定）。
乱数で出題し直しながら繰り返したところ、**12回中3回**再現。はみ出しは毎回ちょうど 8.83px で、出るのはいつも「読み」の問題だった。画像：`samples/round9/1_before_overflow.png`（⑦の「た。」が区切り線を越えて⑧の列に入っている）。

### 原因（実測して確かめたこと）

1. 「読み」の問題は、線を引いた語のすぐ横に答えのマスが付く（`.tgt-wrap` の中に `.side`）。この部分は1列ぶんではなく**約2列ぶんの幅**を占める。そこに文の折り返しが1列加わると、1問ぶんの列幅（A4縦・50問・3段では 11.18mm）を越える。
2. 寸法からの見積り（`layoutFor` の `fits()`）は、この状態を**正しく「入らない」と判定していた**。ところが `layoutFor` は最後に `Math.max(2.8, raw)` で下限に切り上げる。50問の混合ではこの `raw` が 2.5〜2.8 と下限を割るので、**文字サイズは最初から下限 2.8mm に張り付く**。
3. 実測して縮める仕組み（`fittedFont`）の繰り返しは `while (f > 2.8)` だった。文字がすでに 2.8mm なので、**この繰り返しは1回も回らない**。つまり、いちばん厳しい紙面でだけ、実測による修正が丸ごと効いていなかった。段の数を選ぶ `autoLayout` も、下限を割った `raw` どうしの比較で決まるため、実際に収まる並びが選ばれるとは限らない。

（「画面では通るが印刷で出る」という第2・第3節の類の問題ではないことも確かめた。画面の probe・画面の `#printArea`・印刷の `#printArea` の3通りで、同じ8.83pxが出る。）

### 直し方

実測して直す仕組み（`fittedFont`）が、下限に張り付いた紙面でも手を出せるようにした。特定の組み合わせだけ文字を小さくするような場当たりの処置はしていない（第15節で見送った類の対策）。`app.js` の該当箇所：

- `MIN_FONT`（2.8mm）と `HARD_MIN_FONT`（2.2mm）を定数にした（`app.js:1106-1110`）。`layoutFor` の切り上げも `MIN_FONT` を使う（`app.js:1158`）。
- `fittedFont()` を3段階にした（`app.js:1227-1265`）。
  1. いままでどおり、いまの並びのまま実測しながら `MIN_FONT` まで小さくする。
  2. それでもはみ出すときは、**段の数を1〜5段まで変えて実際に描き直し**、はみ出さない並びを探す。試す順は見積りで文字が大きく入る順。段を増やすと1問ぶんの列が広くなるので、折り返した2列目が入るようになる。
  3. どの並びでも収まらないときだけ、`HARD_MIN_FONT` まで下限を下回って小さくする（はみ出したまま印刷して字が隣に食い込むより、そのほうが害が小さい。このとき「マスが小さめです」の注意は従来どおり出る）。
- 選び直した段数を用紙の組み立てに渡すため、`computeLayout(descs, insH, forceTiers)` と `buildSheet(mode, fontMm, forceTiers)` に引数を足し、`renderPaper` `renderPrintArea` `sheetInfoFor` から `fittedTiers` を渡す（`app.js:1169-1177, 1267-1271, 1389, 1452, 1481`）。`recommendCount()`（「約◯問までが書きやすい」の計算）は各問題数ごとに自動で決めたいので、わざと渡していない。
- 「段の数」を手で指定していて実測で変えたときは、プレビュー下に「『並べる段の数』を◯段にすると紙面に収まらないため、◯段にしました。」と出る（従来の同種の案内と同じ形）。
- 最後の 1.5% の余裕（画面と印刷の微差ぶん）は、**下限までしか縮めていないときは下限を割らない**ようにした（従来どおり）。段を選び直して収まった紙面は 2.80mm のまま。

2段階目（段の数の選び直し）も3段階目（下限を割る）も、**実測ではみ出しが出たときにしか動かない**ので、いままで問題なく出ていた組み合わせの見た目は変わらない。

### 確認

すべて Chrome（Playwright、`channel='chrome'`）、`emulate_media('print')` ＋ `page.pdf()` を通したうえで `#printArea` を実測。検査スクリプトは `reviews/round9/`。

- **指摘された組み合わせ（小1〜6・50問・6種類・A4縦）**：乱数で出題し直しながら **77回**（問題用紙のみ25回＋30回、問題用紙＋解答用紙の両方22回）実行し、**はみ出し0件**。修正前に同じ検査をすると再現する（12回中3回）。うち10回前後で2段階目が働き、3段→4段または5段に組み替わった。文字は 2.80mm のまま。画像：`samples/round9/2_after_fixed.png`、PDF：同フォルダ。
- **一般の組み合わせ**：学年5通り×問題数10〜50の5通り×種類11通り×用紙3種から **147通り**を、問題用紙と解答用紙の両方を印刷対象にして実測し、**0件**（`reviews/round9/matrix_after.txt`）。問題数も一致。
- **第17節（A〜K）の回帰**：20項目すべて通過（`reviews/round9/regress.py`）。内訳は、印刷前の自作ダイアログ（既定の焦点・Esc・外側タップ・「このまま印刷する」でだけ `window.print` が1回・警告がないときは出さない・答え漏れのときの見出し）、マスが小さい注意のダイアログ同梱、練習マスの段そろえ（マス数と列の高さが一致）、案内のA4横→A3横の順と家庭用プリンターの注意、送りがなカードの文言、幅1440で用紙選択が1か所・スマホ幅では右上に出る、読みがなの既定（文中の□＝なし／送りがな＝あり）と説明文、学年・組／日付の既定オン、自分で作るで練習マス・なぞりのときだけ例文欄が隠れ入力は残る、区切り線 1px の #999、`.hint-ruby-box` の左余白 7.15px と送りがな側 0px。
- **その他**：50問・全種類の作り直しにかかる時間は最大 0.11秒（段の数を探す処理が入っても体感に影響なし）。「段の数」を手で2段に指定した状態でも、はみ出し0件で案内が出る。

**独立検査は未実施。**

## 19. 用紙サイズの案内が題型によっては逆効果だった（未公開）

第17節(4)で入れた「マスを大きくしたいなら、まずA4横」という案内が、**題型によっては嘘になる**と分かった。**手元で直した（公開はまだ。git には触れていない）。**
変更したファイルは `app.js` と `index.html`（`styles.css` `paper.css` `import.js` `kanji-db.js` は1文字も変更なし）。変更前の6ファイルは `backup-unpublished-20260922f/` に退避。

### 指摘（実測つき）

同じ題組のまま `#gradeFormatSelect` だけを切り替えて解答マスを実測すると：

- 小学1年・15問・「書き」＋「練習マス」混合：A4縦 約7.4mm → **A4横 約6.4mm（小さくなる）** → A3横 約10.5mm。
- 対照：小学3年・10問・「書き」のみ：A4縦 約8.7mm → A4横 約9.1mm（こちらは大きくなる）。

なのに案内は、用紙が何であっても「まずA4横にしてみてください」と無条件にすすめていた。

### 原因

「横にすれば紙が広いから、マスも大きくなる」という**推測**を、固定の文面として書き込んでいた。実際には A4横は A4縦より **87mm低い**（210mm対297mm）ので、練習マス・なぞり書きや、□・線の解答欄のように**下へ積む**題型では、効くのは幅ではなく高さで、横にすると逆に小さくなる。どちらになるかは題組の中身しだいで、固定の順番では決められない。
（同じ確認で、「文中の□」＋「書き」や「自由記述」＋「書き」でも A4横のほうが小さくなる組み合わせが出た。逆に「書き」＋「読み」だけの組では A4横のほうが大きい。）

### 直し方（推測をやめて、実際に測る）

この道具は文字サイズを決めるときも「推定」でなく「実測」している（第18節）。案内も同じやり方にそろえた。`app.js`：

- `fittedFont()` の鍵づくりを `fontKey()` に切り出した（`app.js:1228-1232`）。
- `cellMmForFormat(format)`（`app.js:1384-1407`）：用紙の種類を一時的に入れ替えて `fittedFont()` を通し、**画面外に実際に描いて測った**解答マスのmmを返す。終わったら用紙も内部の記憶も必ず元に戻す（`finally`）。結果は題組＋設定＋用紙ごとに覚えておくので、同じ状態で測り直すことはない。測れなかったときは 0 を返し、その用紙を案内から外すだけで動きは止めない。
- `smallCellText()`（`app.js:1409-1451`）を書き直した。「マスが小さめです」が出るときだけ、いま以外の2つの用紙を測り、
  - **本当に大きくなる用紙だけ**をすすめる（無条件のA4横推しをやめた）。いまの用紙は候補に出さない。
  - 3つとも 7.5mm に届かないときは、正直に「どれも書きやすい大きさ（約7.5mm）には届きません。問題の数を減らすことをおすすめします。」と書く。どれも大きくならないときは「用紙を変えても、あまり大きくなりません（…）。」。
  - どの用紙にすると**何mmになるか**を必ず添える（「A4横で約6.4mm（いまより小さくなります）、A3横で約10.5mmになります」）。変化が0.3mm未満なら「（ほとんど変わりません）」。
  - 7.5mm に届く用紙が2つあるときは、家庭のプリンターで印刷できるA4のほうを先にすすめる。
  - A3が本当に大きくなるときだけ「家庭用プリンターでは印刷できないことがあります（コンビニなどの印刷が必要です）」を添える。
- `index.html` の静的な案内（`.size-hint`、出題**前**に出るので実測できない）は、断定をやめた：「まず用紙をA4横にして試してみてください。多くの場合は同じ問題数でも大きくなりますが、練習マス・なぞり書きのようにマスを下へ積む種類では、かえって小さくなることもあります」＋「問題を作ったあとは、用紙を変えるとマスが何mmになるかを実際に測って案内します」。

### 確認（Chrome／Playwright、`channel='chrome'`）

- **案内の数字が実測と一致するか**：12通りの題組（書き＋練習マス／書きのみ／文中の□／自由記述＋書き／なぞり書き／読み＋書き／練習マス＋なぞり／全6種50問 ほか）について、3つの用紙それぞれで案内文を採り、そこに書かれたmmを**実際にその用紙へ切り替えて測った値**と突き合わせた。**ずれ0件**（許容0.15mm）、自分の用紙を案内に挙げた例も0件、コンソールのエラーも0件。
- **指摘の2例**：小1・15問・書き＋練習マス → 「用紙を変えると、A4横で約5.7mm（いまより小さくなります）、A3横で約9.6mmになります。A3横にすると、書きやすい大きさになります。」（修正前は「まずA4横にしてみてください」）。対照の書き＋読みの組では、A4横が7.5mmを超えるときに「用紙を変えると、A4横で約7.7mm、A3横で約11.6mmになります。まずはA4横にしてみてください。」と、**従来どおりA4横をすすめる**ことも確認。
- **どれも届かない場合**：小1・30問・書き＋練習マス（A4縦5.0／A4横4.9／A3横5.0mm）で「用紙を変えても、あまり大きくなりません（A4横で約4.9mm、A3横で約5.0mm）。問題の数を減らすことをおすすめします。」。50問・全6種では「…になりますが、どれも書きやすい大きさ（約7.5mm）には届きません。」。
- **第17節・第18節の回帰**：`reviews/round9/regress.py` の20項目すべて通過（印刷前の自作ダイアログ一式を含む。ダイアログの中身も**新しい案内の全文**が入っていることを確認）。印刷メディアでのはみ出し検査 `reviews/round9/matrix.py` を147通り（問題用紙＋解答用紙）実行し、**0件**。
- **操作の速さ**：50問・全6種で、出題し直し 133ms、種類の切り替え 24〜32ms、問題数の切り替え 35ms、用紙の切り替え 33〜67ms。追加の実測は「マスが小さめです」が出るときだけ走り、同じ状態では覚えた値を使うので、体感の遅れはなし（0.3秒の目安を大きく下回る）。

**独立検査は未実施。**
