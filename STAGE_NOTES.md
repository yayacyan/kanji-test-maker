# 漢字テストメーカー — 階段記錄（2026-09-20）

這份是「做到哪、怎麼用、驗證了什麼、還沒驗證什麼」的存檔，給之後回來接手時看。
程式本身沒有任何依賴，也不需要建置。

## 1. 怎麼開啟

```bash
cd kanji
python3 -m http.server 8765
# 瀏覽器開 http://127.0.0.1:8765/index.html
```

- 以上是實際驗證過的開法。**直接雙擊 index.html（file://）開啟未驗證**。
- 手機測試：桌面上開好之後，用同一個 Wi-Fi 的手機開 `http://<這台電腦的IP>:8765/`（未實測）。
- 資料存在瀏覽器的 localStorage（key：`kanji-test-maker-v2`）。換瀏覽器、換裝置不會帶過去。
  舊版的 `kanji-test-maker-v1` 資料會在第一次開啟時自動搬過來。

## 2. 檔案

| 檔案 | 內容 |
|---|---|
| `index.html` | 畫面骨架（編輯區＋預覽區＋底部分頁） |
| `styles.css` | 畫面（手機優先）與印刷 CSS。試卷尺寸全部用 mm |
| `app.js` | 狀態、抽題、題目清單編輯、試卷組版、預覽、列印 |
| `import.js` | 貼上文字／AI 輸出／OCR 結果的解析，以及 OCR 的替換介面 |
| `kanji-db.js` | 題庫：1026 字（`window.KANJI_DB_ROWS`） |
| `CLAUDE.md` | 原本就有的（教師人設），沒有動 |

`.claude/settings.local.json` 是工具的權限紀錄，不是本專案的一部分。

## 3. 功能一覽

- **取り込み（OCR／AI）**：上傳或拍照 → 先出現「可編輯的辨識結果」→ 下方即時顯示解析預覽（每行可勾選排除）→ 按「題目に追加」才會變成題目。
  可解析：`漢字（よみ）／例文`、`漢字,よみ,例文`、`漢字、読み、例文`、`花の（　）が出る。|芽`、純漢字清單、`①巣（す）` 這類編號行、JSON（含 ```json 圍欄）。
  AI 用プロンプト可複製（不含任何 API key）；可加「テーマ」。
- **抽題**：小1〜小6、小1〜3、小1〜6；10/20/30/50 題；題型可複選（書き／読み／例文穴埋め／練習マス／自由入力，複選時交互出）。
  同一漢字不重複；跨學年範圍會盡量平均分配各學年。預設「前回の抽題を入れ替える」＝只換掉上次抽題的題目，自作／OCR／AI 的題目保留。
- **自作**：單題新增，或批次貼上。清單內每題可編輯（題型、解答欄=マス/かっこ/線、漢字、よみ、例文、答え、學年）、↑↓、複製、刪除、シャッフル、學年順、全削除。
- **試卷**：直書；A4 縱／A4 橫／A3 橫；問題紙／解答紙（答案紅字 #d22）；右下「漢字テストメーカー」。
  段數自動：A3 橫 ≤10題=1段、≤20=2段(各10)、其餘=4段；A4 橫 ≤14=1、≤28=2、≤42=3、其餘=4；A4 縱 每 10 題 1 段（最多 5 段）。也可手動指定。
  設定：標題、名前欄、點數欄、書體（教科書體／明朝／ゴシック）、例文穴埋めの読みヒント、問題紙＋解答紙連續列印。
- **手機**：單欄；底部分頁「編集／プレビュー／解答」；第一屏有 4 個大按鈕（AI・OCR取込／学年から抽題／手入力／プレビュー）；輸入框與按鈕 ≥44px；預覽可橫向捲動、可放大縮小。
- **列印／PDF**：列印時只輸出 `#printArea`（不含畫面 UI），`@page` 依所選尺寸動態設定。PDF 用瀏覽器的「儲存為 PDF」（沒有另做 PDF 按鈕）。

## 4. 資料結構

題目（存 localStorage，也是之後接後端時的介面）：

```js
{ id, type, grade, kanji, reading, sentence, answer, style, source, base }
// type   : kaki | yomi | sentence | mas | free
// style  : mas | kakko | line
// source : bank(抽題) | ocr | ai | paste | custom
// base   : 抽題時的配當漢字 1 字（去重用）
// 約定：sentence 一律是「完整句」，kanji 是句中出現的出題語（空欄符號會在輸入時填回答案）
```

題庫一列：`[學年, 配當漢字, 出題語, 出題語的讀音, 例文]`。要換成資料庫／JSON，只要換掉 `kanji-db.js` 的陣列。

## 5. OCR 的現況

- 介面：`import.js` 的 `runOcr(file, provider, hooks, opts)`（也掛在 `window.runOcr`）；`OCR_PROVIDERS` 裡加一個 `{label, run(file,hooks,opts)→{text}}` 就能接 Google Cloud Vision 等外部 API（檔內有註解範例）。
- 已內建：`tesseract`（Tesseract.js，從 CDN 載入，需要網路，可勾「縱書き」用 jpn_vert）、`mock`（示範，不讀圖片）。
- 實測：Tesseract 讀了一張日文圖，約 2 秒，文字大致正確，但會把「／」讀成「プア」「ノ」——所以設計成「先編輯再轉題目」，解析器也容忍 `ノ 丿 |` 當分隔。
- **未實測**：手機實拍照片、縱書き圖片、大張照片的速度。

## 6. 題庫的來源與品質

- 學年配當：Wikipedia「学年別漢字配当表」（令和2年度〜，80/160/200/202/193/191＝1026 字），用另一份資料集交叉比對，差異剛好是 2020 改訂的部分。
- 例文：**AI 分學年撰寫**，機械檢查全數通過（漢字集合完全一致、出題語在句中恰好一次、讀音為平假名、句中漢字不超過該學年、句子不重複）。
- 日文二讀（另一個 agent 看完 1026 件）：找到 5 件要修，已改（短の讀點、旧友→旧式、仁→仁術、徒歩／自宅的「十分」→「十五分」）。
- **仍請人工過目**（AI 二讀認為「不算錯，但由老師判斷」）：
  - 內容敏感度：死（花は死んでしまう）、血、酒、殺（殺虫）、墓、亡、党、皇／后／陛、仏、宗（宗谷）。
  - 兩字共用同一出題語（書き問題時答案相同）：岐／阜＝岐阜県、沖／縄＝沖縄県、滋／賀＝滋賀県、研／究＝研究。
  - 用語習慣：老人（お年寄り）、生徒会（児童会）。
- 產生題庫用的暫存腳本在工具的 scratchpad（`/private/tmp/...`），之後可能消失；**以 `kanji-db.js` 為準**。

## 7. 驗證紀錄（怎麼確認過）

自己跑（Playwright＋系統 Chrome）：375／768／1440px 都無橫向溢出；所有按鈕輸入框 ≥44px；全題型×問題紙／解答紙截圖；A3 橫 20 題=[10,10]、50 題=[13,13,13,11]；`page.pdf` 的 MediaBox 為 A4 595×842、A4 橫 842×595、A3 橫 1191×842 且頁數正確；抽題無重複；HTML 跳脫（塞 `<img onerror>` 不會執行）；舊版資料搬移。

獨立驗收（fresh-context agent，只給條件不給過程）：
1. 第一輪：條件 1〜8 全通過，另指出 5 個問題（自由入力解答被切、9 字以上答案缺字、小1〜2 題目混入「使」、解答線太短、表頭行被當題目）→ 已修。
2. 再檢查：再抓到「文字大小用推算，長文折行列數估少 → 解答欄被擠出去」→ 改成「畫面外實際描繪、量到不溢出為止才縮小字級」（`fittedFont()`，下限 2.8mm）。檢查者的重現腳本與自己的回歸測試都通過。
3. **最後這次修正，沒有再交給獨立 agent 整體重驗**（只用重現腳本＋自己的回歸測試確認）。

## 8. 已知小問題 / 尚未確認

- 實機（iPhone／Android）、iOS Safari 的列印、真實相機照片的 OCR：沒測。
- 空白分隔的表頭行「漢字 よみ 例文」仍會被當成一題；加引號的 CSV 表頭不會被自動排除（預覽會顯示，可取消勾選）。
- OCR 結果會把「（　）」中的全形空白吃掉（仍可正確辨認為空欄，不影響使用）。
- 例文穴埋め的「読みヒント」字很小（可在設定關掉）。
- 題庫 50 題塞在 A4 縱時字級會降到約 3.2mm（約 9pt）；想更大請改用 A3／減少題數／手動減少段數。

## 9. 之後可以做的（沒做，需要時再說）

- 自製題庫匯出／匯入（JSON 備份，換裝置用）。
- 題目「題庫以外」的字（中學漢字）的支援與提示。
- 接雲端 OCR（在 `OCR_PROVIDERS` 加一個即可）。
- 直接產生 PDF 檔的按鈕（目前依賴瀏覽器列印）。

## 10. 這次學到的（已寫入 `~/.claude/rules/lessons.md`）

- 折行會改變排版的自動組版，不要用推算，要實際描繪後量測。
- 驗收要用「多次隨機抽題×極端長文」，並要求驗收者留下重現腳本。
- 在 `cd 目錄 && …` 之前先確認目錄存在，否則會弄髒目前所在的資料夾。


---

## 11. 第2輪の変更（2026-09-20 夜）— 3 つの評価報告（`REVIEW_NOTES.md`）への対応

**追加・変更した機能**
- 画面の用語をやさしい日本語に統一（抽題→問題を作る、題目→問題、題型→問題の種類、問題紙/解答紙→問題用紙/解答用紙、複製→コピー、OCRエンジン→読み取り方法〔折りたたみ〕 など）。
  ※ 問題の種類の名前（書き問題・読み問題・例文穴埋め・練習マス・自由入力）は、最初のご依頼の名前のまま。
- **用紙**：文字の上限を引き上げ（A4=8mm、A3=10mm）。A4は「文字が大きくなる段数」を自動で選ぶ（A3 は仕様どおり 20問=2段×10、21問以上=4段）。10問A4縦は2段で解答マス約8〜10mm。
  プレビュー下に「解答マス 約○mm／文字 約○pt」を表示し、7.5mm未満なら注意書き。
- **説明文**：用紙の上に「問1〜20 線のひいてある ひらがなを、漢字で書きましょう。」等を自動で入れる（設定でオフ）。小1・2だけの用紙はかな中心の文にする。
- **種類ごとの問題数を指定**（例：書き20＋読み10）。作った問題は種類ごとにまとまって並び、まとまりの中は学年の低い順。
- **ヘッダ**：見出しの補足、学年・組、日付、1問あたりの点数（合計「／◯点」）。解答用紙には名前・点数欄を出さない。
- **漢字だけの入力**（貼り付け・自作フォーム）で、小学校の漢字なら読みと例文を自動で補う。区切りなしの「巣歩観」は1字ずつに分ける。題庫にない漢字は注意を出す。
- **OCR誤読対策**：「／」の読み違い（プア・ノ 等）を吸収。怪しい先頭文字は警告して初期はチェックを外す。
- **保存・読み込み**：JSONファイルに書き出し／読み込み（別の端末へ移す用）。保存表示は「この端末に保存済み」。
- **首次画面**：見本を自動で入れない。「問題を作る」ボタンを画面下に固定（375×667でもスクロールなしで押せる）。手機のプレビューは右端（1問目）から表示。ズームは「−・％・＋・画面に合わせる」。
- **印刷**：ボタンに「問題用紙／解答用紙／両方」を小さく表示。0問なら印刷しない。
- **問題づくり**：同じ語が1枚に2回出ない（岐阜県など）。書き取りの答えが別の問題文にそのまま出るのを減らす。「以前に出した漢字はなるべくさける」と、その記録を消すボタン。
- **書体**：字型名の引用符でstyle属性が壊れ、常にゴシックになっていた不具合を修正。この端末に教科書体がなければ、選択肢に明記。
- 自由入力：書く欄を2本の線に（列が狭いときは1本）。線の長さは答えの長さと無関係。解答用紙の範例は「（例）」付き。
- 十字の補助線を実線に（解答用紙では消す）。

**この輪で見つけて直した不具合（重要）**
- **印刷でだけ解答欄が切れる**：印刷用の用紙が画面より0.6mm低く、画面で測った「収まる文字サイズ」が印刷では収まらなかった。画面と印刷の高さをそろえ、測定も印刷メディアで確認するようにした（教訓簿に記録）。
- 自由入力50問など極端な条件での書く欄のはみ出し、答え12字超の欄のはみ出し。

**まだ残っていること（次の候補）**
- 30〜50問のA4は解答マスが4〜6mm（マスの大きさを直接指定する機能はない）。長文（40〜60字）×50問のような極端な組み合わせでは、下限文字サイズでも列から出る。
- 題庫：潮（「海の潮」が重複表現）、辞典／事典、亡くなった／無くなった、名まえ／名前、友だち／友達・子ども／子供の別解、皇后 など、評価で指摘された例文は未修正。別解を登録する機能もない。
- 複数のテストの保存・履歴、A／B版の自動作成、生徒別管理、解答の縮刷版、除外指定、県名の除外。
- 大問見出しは用紙の最上部にまとめて出る（各問題の直上ではない）。段替えの案内はなし。
- 実機（iPhone/Android）、実プリンタ、実写真のOCRは未確認。

## 12. 第3輪の変更（2026-09-20 夜）— 「方格が問題の横にない」「題型を増やして」への対応

**きっかけ**：ご指摘①「方格が題目の旁邊にない」②「題型はこれだけ？参照サイト（kanjitestmaker.cococig.com）を見て増やして」。

**参照サイトを実際に開いて確認したこと**
- 参照サイトの問題の種類は **「書き」と「読み」の2つだけ**（最大20問、画像として保存）。題型が多いサイトではない。
- ただし用紙のつくりが違った：方格は**文の中の、その言葉の位置**にあり、読みが方格の横に小さく付く。「読み」は言葉の横に線を引く。
- 私の用紙は、書き・読みの方格が「列の上端の別の場所」にあり、言葉とずれていた。→ これが①の原因。

**変更**
- **方格を言葉のすぐ左横に付けた**（書き〔線のひらがな〕・読み〔線の漢字〕）。方格は文の流れの中に入れているので、長い文が2列に折り返しても言葉の横に付く。答えを書く場所（マス・かっこ・線）の選択も、そのまま横の欄に効く。
- 参照サイトの「書き」と同じ形（文中の言葉が□になり、読みが横に付く）は、既存の種類「例文穴埋め」がそのものだったので、名前を **「書き（文中の□）」** に変えて分かりやすくした。
- **新しい種類を2つ追加**（合計7種類）：
  - **送りがな**：「借りる」→「借」は見せ、送りがなの数だけ□、全体に読み「かりる」。答えは「りる」。出題は「漢字1字＋送りがな（3字まで）」の語だけ（262語。「王さま」「子ども」「友だち」など送りがなでない語は除外）。小1は語が少なく、21問までになる（画面にその旨を表示）。
  - **なぞり書き**：お手本の漢字の下のマスに薄い灰色の字が入っていて、なぞってから書く（低学年向け）。
- **全部の問題の種類をまとめて変える**ボタン（参照サイトの「すべて読み問題に変更」に相当）。自由入力は変えない。送りがなに変えるときは、送りがなのない語をそのままにして知らせる。
- 種類名：書き（線のひらがな）／読み（線の漢字）／書き（文中の□）／送りがな／練習マス／なぞり書き／自由入力。

**確認したこと**
- 既存テスト（t_fix, t_layout, t_print, t_ui, t_r2, t_regress2）はすべて通過。
- 新規：全7種類を混ぜて A4縦10/30/50・A4横20/50・A3横20/50 × 6回（乱数出題、方格の様式もランダム）を、画面・印刷、問題用紙・解答用紙で測定 → はみ出し0、方格の位置ずれ0、方格と文字の重なり0（方格2,000件超）。
- 同じ問題で新旧を比べた文字サイズ：10問は大きくなり、20〜50問は最大約1割小さくなる（方格の横幅が増えたぶん）。

**独立検査（別の担当）の結果**：A（方格の位置）・B（新しい種類）・C（はみ出し：画面・印刷・PDF）・D（スマホ375px・44px・コンソールエラー0・自動保存）・E（日本語）すべて通過。指摘された点のうち次を直した：
- 説明文の「薄い字」→「うすい字」（「薄」は小学校で習わない漢字）。
- なぞり書きで、漢字が1字のときも薄い字を2マス入れる（以前は1マス）。
- 句点「。」が方格や語から離れる／次の列の先頭に落ちる：語・方格の直後の句読点を、その語・方格と一体にした。
- 直さなかった点：語の後ろに方格ぶんの空きができる（上に記載）／例文が30字を超える極端な条件×A4の50問では、最小文字サイズ2.8mmでも切れる（実データの最長は21字）／PDFの内容を座標で機械検査はしていない（目視のみ）。

**ためしたが採用しなかったこと**
- 方格を文の高さに数えず、後ろの文字の左横にはみ出させる案：列の下端で方格が外に出て、全体の文字が小さくなりすぎた（マス4.5mm）ため戻した。代わりに、読みが長い問題（例：ひるやすみ＝5マス）は文に少し空きができる。

**まだ残っていること**
- 参照サイトの「名前を付けて画像を保存／画像をコピー」（PNG出力）は未対応（印刷／PDF保存は可能）。
- 読みの欄が長い問題は、文の途中に空きができる。
- 「同音異字」「部首」「画数」「対義語」などは、正しいデータがないため作っていない（データを用意すれば追加できる）。
- 第2輪までの「残っていること」は §11 のとおり。

## 13. 第3輪の最終調整（2026-09-20 夜）— 4人の評価（`REVIEW_NOTES.md` 9章）への対応

変更の一覧は `REVIEW_NOTES.md` の9章「そのほか直したこと」のとおり。ファイルごとの主な変更：
- `app.js`：最初のプレビューを用紙全体表示に／通知を上部・長めに／警告（同じ問題・答えが見える・マスが小さい目安・段数の自動調整）／見本の読み込み／名前付きセット（localStorage `kanji-test-maker-sets`）／元に戻す／戻るボタン（history）／練習マスのマス数／送りがなの語の選別
- `import.js`：「借（か）りる／例文」の貼り付け形式、例文の「／」の警告
- `index.html`：1画面目の説明文、見本ボタン（プレビューが空のとき）、名前つき保存の欄、印刷のヒント、警告表示欄
- `styles.css`：通知の位置、□の大きさと補助線、スマホの保存表示、追加部品
- `samples/`：見本用紙の画像（7種類・1段／2段）。作り直し用の手順は STAGE_NOTES 12章のとおり PDF から画像化した（画面のスクリーンショットは右端が切れるため）

確認：既存テストは全通過。新規テスト（`t_final.py`：スマホの初回プレビュー・戻るボタン・貼り付け・重複と答え漏れの警告・段数の自動調整・練習マス・名前付きセット・フォント）は全項目通過。別担当の独立検査（17条件）は16通過・1不通過（通知内ボタン36px）→ 44pxに直して再確認済み。詳細は REVIEW_NOTES 9章。
